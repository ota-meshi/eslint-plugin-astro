---
const { class: className = "" } = Astro.props
const { variant = "primary" } = Astro.props
const { before, after } = Astro.slots
---

<span
  class:list={[
    `link pixel variant-${variant}`.trim(),
    `${before ? "has-before" : ""}`.trim(),`${
      after ? "has-after" : ""
    }`.trim(),`${className}`.trim(),
  ]}
>
</span>
